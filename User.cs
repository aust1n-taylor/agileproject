using System;

public class User
{
	public User()
	{
		public string name;
		public string title;
		public string password;
		public int privilageLevel; // ) lowest, 2 highest (for now, can be changed). 


	public User(string name) {
		this.name = name;
	
	}
	}
}
